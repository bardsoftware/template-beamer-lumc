%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Beamer Presentation
% LaTeX Template
% Version 2.0 (10/06/16)
%
% Attention:
% 0. The self-defined font is used, because 'Calibri' is 
% not supported in the latex font packages. 'LuaLatex'
% should be used.
% 1. This template has been generated according to 
% the Power Point template of LUMC in 2016. 
% 2. This is generated purely with images as the 
% background.
% 3. The bullet point color was used purely for personal 
% preference. 
% 4. Any more adding to the template are welcome. 
% 5.In order to use the navigation bar, the title 
% for each section should not be to long. 
% 6.Adding animation is possible. I prefer to add another
% pdf file with:
% \animategraphics[parameters]{1}{fname}{startnum}{endnum}
% 7. This is my first template, the files might be not  
% well organized, sorry for that. 
% 
% Author:
% Shengnan Liu
% sliu729@gmail.com
% Division Medical imaging processing, 
% Leiden University Medical Center
% 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Modification Log:
% Generated by Shengnan Liu on 21-01-2016
% Cleaned up for further usage on 10-06-2016
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\mode<presentation>
\RequirePackage{tikz}
\usepackage[yyyymmdd]{datetime}
\usepackage{epstopdf}
\renewcommand{\dateseparator}{-}
\geometry{papersize={254mm,190.5mm}}
\useinnertheme{lumc}
\useoutertheme{lumc}
\usecolortheme{lumc}
\usefonttheme{lumc}

%%% Here is some self defined code...
%Navigation bars
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{blocks}[rounded][shadow=true]  
\usepackage{tikz}
\usetikzlibrary{arrows,positioning} 
\tikzset{
    %Define standard arrow tip
    >=stealth',
    %Define style for boxes
    invisible/.style={opacity=0},
    visible on/.style={alt={#1{}{invisible}}},
    alt/.code args={<#1>#2#3}{\alt<#1>{\pgfkeysalso{#2}}{\pgfkeysalso{#3}} % \pgfkeysalso doesn't change the path
  },
    punkt/.style={
           rectangle,
           shade,
           top color = red!0!green!31.693989!blue!68.30601,
           %top color = red!0!green!45.69!blue!54.31,
           %bottom color = red!0!green!31.693989!blue!68.30601,
           rounded corners,
           draw=blue!40!black!60, very thick,
           color = white, very thick,
           text width=10cm,
           minimum height=2cm,
           text centered},
    % Define arrow style
    pil/.style={
           ->,
           thick,
           shorten <=2pt,
           shorten >=2pt,}
}
%% flow charts 
\newcommand{\studygoal}
{
\definecolor{mygreen}{RGB}{12, 66, 12}
\definecolor{myred}{RGB}{118,4,4}
\definecolor{myyel}{RGB}{162,99,0}
\begin{center}
\begin{tikzpicture}[node distance=2cm, auto,]
 %nodes
 \begin{scope}
 \node[punkt,bottom color=myred,visible on=<2->] (angle) {\huge light incident angle};
 \node[punkt,bottom color=myred,visible on=<2->,inner sep=5pt,below=0.5cm of angle]
 (distance) {\huge The distance between the light source and the arterial wall};
 % We make a dummy figure to make everything look nice.r
 \node[above=of angle] (dummy) {};
 \node[punkt,bottom color=myyel, right=of dummy,visible on=<4->] (t) {\huge The catheter position relative to the arterial wall}
   edge[pil,->,bend left=45,visible on=<4->] (angle.east) % edges are used to connect two nodes
   edge[pil,->, bend left=45,visible on=<4->] (distance.east); % .east since we want   % consistent style
 \node[punkt,text width = 7cm,visible on=<1->, 
 bottom color=mygreen,left=of dummy](g) {\huge IVOCT intensity}
   edge[pil,<-, bend right=45,visible on=<3->] (angle.west)
   edge[pil,<-, bend right=45,visible on=<3->] (distance.west)
   edge[pil,<-, bend left=45,visible on=<5->] node[auto] {} (t);
\end{scope}
\end{tikzpicture}
\end{center}
}





\mode<all>